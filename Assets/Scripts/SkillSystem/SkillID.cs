using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// each skill has its own unique ID
public enum SkillID
{
    TELEPORTATION,
    ZAP,
    BOMB,
    PUNCH,
    MOVE,
    PUSH,
    SWAP,
    FROZA,
}